# [실행 계획서] 전국 세척기 통합 관리 및 서비스 플랫폼 구축

> [!NOTE]
> **진행 상황 관리**: 실시간 작업 현황은 [work_schedule.md](work_schedule.md)에서 확인하세요.
> **사용자 준비 가이드**: 서비스 가입 및 행정 절차는 [human_preparation_guide.md](human_preparation_guide.md)를 참고하세요.

본 문서는 프로젝트의 전반적인 방향성과 상세 스펙을 정의하는 마스터 청사진입니다. 주기적으로 업데이트되며, 모든 개발 및 운영 의사결정의 기준이 됩니다.

---

## 1. 프로젝트 개요 및 목적
본 프로젝트는 전국 단위의 세척기 판매, 수리(A/S), 세제 공급 및 부품 관리 사업을 디지털로 전환하는 것을 목적으로 한다. 식당(소비자)의 니즈를 실시간으로 수집하고, 전국 지사망을 통해 즉각적인 현장 서비스를 제공하며, 본사(마스터)가 전체 운영 현황과 실적을 데이터 기반으로 관리할 수 있는 통합 FSM(Field Service Management) 플랫폼을 구축한다.

## 2. 사용자별 핵심 기능 및 인터페이스 구성

### 2.1 소비자 (식당용 모바일 앱/웹)
- **원터치 서비스 요청**: 세척기 고장 수리, 세제 보충, 부품 교체 등 요청 사항을 사진/동영상과 함께 등록.
- **실시간 진행 현황 확인**: 접수 완료, 지사 출동 중, 수리 완료 등 서비스 전 과정을 앱에서 실시간 모니터링.
- **통합 결제 시스템**: 서비스 완료 후 수리비, 부품비, 세제 대금을 앱 내에서 직접 결제 (신용카드, 간편결제 등).
- **로열티 프로그램**: 결제 금액 기반 포인트 적립 및 재방문/재구매 유도를 위한 쿠폰 및 바우처 자동 발행.

### 2.2 지역 지사 (현장 대응용 모바일 앱)
- **실시간 긴급 알림**: 관할 지역 식당의 요청 발생 시 즉각적인 푸시(Push) 알림 수신.
- **출동 및 이력 관리**: 요청 수락 후 현장 방문, 조치 내용(사용 부품, 수리 내역) 등록 및 관리.
- **지사별 실적 대쉬보드**: 본인 지사의 매출 실적, 처리 건수, 정산 예정 금액 확인.

### 2.3 본사/마스터 (관리자용 PC 웹)
- **실시간 통합 관제**: 전국 지사의 대응 현황을 지도 및 리스트 형태로 모니터링.
- **SLA(Service Level Agreement) 관리**: 지사의 대응이 설정된 시간보다 늦어질 경우 경고 알림 표시.
- **데이터 분석 및 통계**:
  - 주별/월별/년별 지사별 매출 및 문제 유형 통계.
  - 동일 업체/지역의 반복적 고장 이슈(Hotspot) 분석 및 보고.
- **정산 관리**: 매주 단위로 발생하는 결제 대금에 대해 본사 수수료를 제외한 금액을 지사에 정산하는 기능.

## 3. 기술 스택 및 인프라 설계

### 3.1 소프트웨어 (SW)
- **Mobile App**: Flutter 기반 하이브리드 개발 (iOS/Android 동시 대응 및 실시간 푸시 기능 최적화).
- **Admin Web**: React.js 기반의 고성능 데이터 시각화 대쉬보드.
- **Backend**: FastAPI (Python) 기반의 고성능 API 서버.
- **Database**: PostgreSQL (고객 정보, 결제 이력, A/S 히스토리 관리).
- **Real-time**: Firebase Cloud Messaging (FCM)을 활용한 즉각적인 알람 시스템.

### 3.2 Infrastructure (AWS Cloud - ap-northeast-2)
- **Compute**: Amazon EC2 (Ubuntu 22.04 LTS) + Docker 기반 컨테이너화 운영.
- **Database**: Amazon RDS (PostgreSQL 15+) - Private Subnet 배치로 외부 접근을 차단하고 EC2 보안 그룹(SG)과만 연동.
- **Storage**: Amazon S3 (수리 전/후 사진 및 멀티미디어 파일 저장).
- **Networking & DNS**: 
  - **Route 53**: `whiteon` 자체 도메인 관리 및 DNS 라우팅.
  - **ACM (AWS Certificate Manager)**: SSL/TLS 인증서를 통한 HTTPS 암호화 통신 강제 적용.
  - **VPC**: 퍼블릭/프라이빗 서브넷 분리를 통한 네트워크 보안 계층화.
- **Hybrid Node**: 대규모 데이터 연산 및 통계 분석을 위해 기존 고성능 로컬 서버(Ryzen 9/Threadripper)를 로컬 분석 노드로 병행 활용.

### 3.3 Communication Logic (3-Way Ecosystem)
세 가지 주체 간의 실시간 데이터 흐름을 정의한다.
- **식당 사장님 (Owner)**:
  - 수리/세척/세제 요청 발생.
  - 실시간 기사 위치 및 도착 예정 시간(ETA) 조회.
  - 서비스 완료 후 결제 및 환불 요청.
- **지사/기사 (Branch/Driver)**:
  - 관할 지역 배정 알림(FCM Push) 수신 및 수락.
  - 수리 전/후 리포트 작성 및 사진 업로드.
  - 정산 내역 및 업무 스케줄 확인.
- **본사/HQ (Master Dashboard)**:
  - 전체 주문 흐름 및 지사별 가동률 관제.
  - 분쟁 발생 시 중재 및 정산금 확정/지급 승인.
- **통신 방식**: 
  - **REST API**: 기본적인 CRUD 및 비즈니스 로직 처리.
  - **WebSockets/FCM**: 배정 알림, 기사 위치 이동 등 실시간 상태 업데이트.

### 3.4 Security & Compliance
- **Security Groups**: 80/443 포트 외 외부 접근 전면 차단. DB 포트(5432)는 VPC 내부 EC2 소스에 대해서만 허용.
- **B2B Tax Automation**: 정산 단계에서 지사/식당 간 세무 처리를 자동화하기 위해 국세청 연동 전자세금계산서 API 연동 계획.
- **Data Privacy**: 개인정보보호법 준수를 위한 DB 암호화 및 접근 제어(IAM) 적용.

## 4. 비즈니스 모델 및 결제/정산 로직

### 4.1 결제 시스템 (PG 연동)
- **수수료 최적화**: 구글/애플의 인앱 결제 수수료를 피하기 위해 국내 PG사(포트원 등) API를 직접 연동.
- **결제 수단**: 신용카드, 계좌이체, 카카오페이, 네이버페이 등 지원.

### 4.2 수수료 및 정산 구조
- **정산 주기**: 주 단위 정산 (추후 운영 상황에 따라 월 단위 확장 가능하도록 유연하게 설계).
- **정산 흐름**:
  1. **식당 결제**: 서비스 완료 후 앱에서 결제 (본사 계좌 취합).
  2. **매출 분류**: 시스템이 지사별/건별 매출을 자동 분류.
  3. **본사 승인**: HQ 대시보드에서 정산 데이터 검토 및 지급 승인.
  4. **지급 및 증빙**: 본사 수수료 차감 후 지사 계좌 입금 및 자동 전자세금계산서 발행.

---

## 5. 단계별 실행 로드맵 (상세 실행 계획)

> [!IMPORTANT]
> **문서화 규칙**: 모든 세부 단계(Phase X.X) 착수 전에는 반드시 `plan_phaseX.X_description.md` 형태의 실행 계획서를 먼저 작성하여 히스토리와 설계 의도를 남긴다.

### Phase 1: 데이터 뼈대 및 백엔드 인프라 구축 (Backend & DB First)
* **1.1 DB 스키마 설계 (PostgreSQL):** 식당 정보, 지사 코드, 접수 이력, 부품 재고, 결제 내역을 담을 테이블 및 관계성(ERD) 설계.
* **1.2 코어 API 개발 (FastAPI):** 식당의 A/S 요청을 지사에 할당하고 결제 상태를 업데이트하는 핵심 비즈니스 로직 개발.
* **1.3 로컬 테스트베드 세팅:** 현재 구축되어 있는 고성능 로컬 서버(Ryzen 9 9950X / Threadripper 7970X 환경)를 활용하여 대용량 트래픽 및 데이터 파이프라인 처리 테스트 환경 구성.
* **1.4 외부 API 사전 연동:** 포트원(PortOne) PG사 가입 및 샌드박스 연동, Firebase(FCM) 프로젝트 생성.

### Phase 2: 현장 데이터 수집용 모바일 앱 MVP 개발 (App MVP)
* **2.1 식당(소비자)용 앱 (Flutter):**
    * 카테고리 선택형 A/S 접수 폼(사진 업로드 포함) UI 구현.
    * PG사 API를 통한 결제(신용카드/간편결제) 모듈 연동.
* **2.2 지사용 앱 (Flutter):**
    * FCM 기반 실시간 긴급 출동 푸시(Push) 알림 수신 로직 구현.
    * 접수 건에 대한 '출동 수락' 및 조치 후 '수리 완료(부품/세제 차감)' 상태 변경 기능.

### Phase 3: 기능 고도화 및 보안 강화 (Refinement & Security)
* **3.1 사용자 인증 및 권한 시스템 (Auth):**
    * JWT 기반 보안 로그인 구현 (식당용, 지사용, 본사용 권한 분리).
    * 지사별 관리자 계정 승인 및 관리 로직.
    * **[추가]** 서비스 이용약관 및 개인정보처리방침 초안 작성.
* **3.2 멀티미디어 데이터 처리:**
    * 수리 전/후 사진 및 동영상 업로드 기능 (`POST /media` 연동).
    * 이미지 리사이징 및 클라우드 스토리지(S3 등) 최적화 저장.
* **3.3 본사 관리 대시보드 (Web):**
    * 전국 지사 출동 현황 실시간 지도 관제 및 통계 그래프.
    * 주 단위 자동 정산 리포트 생성 기능.

### Phase 4: 상용 AWS 인프라 및 보안 구축 (Production Ready)
* **4.1 AWS 인프라 셋업:**
    * VPC(ap-northeast-2), EC2(Ubuntu 22.04), RDS(PostgreSQL 15) 인스턴스 생성 및 보안 그룹 설정.
    * Docker를 이용한 FastAPI 서버 컨테이너 배포 환경 구축.
* **4.2 도메인 및 SSL/TLS (HTTPS) 적용:**
    * Route 53을 통한 `whiteon` 도메인 연결 및 ACM SSL 인증서 발급/적용.
    * Nginx(또는 AWS ALB)를 통한 HTTP -> HTTPS 강제 리다이렉션 설정.
* **4.3 3-Way 실시간 통신 연동:**
    * FCM(Firebase Cloud Messaging) 및 WebSocket 서버를 활용한 앱간 실시간 상태 동기화 구현.
* **4.4 세무 자동화 API 사전 검토:**
    * B2B 정산을 위한 전자세금계산서 API 연동 규격 확인 및 테스트.

### Phase 5: 네이티브 앱 변환 및 최적화 (Mobile Engineering)
* **5.1 Capacitor 프로젝트 구성:**
    * 웹 코드(HTML/JS)를 안드로이드/iOS 네이티브 프로젝트로 변환.
    * 앱 아이콘, 스플래시 화면(로딩 화면) 디자인 및 적용.
* **5.2 네이티브 기능 연동:**
    * Firebase(FCM) 기반 실제 스마트폰 알림바(Push Notification) 연동.
    * 카메라 API 및 GPS 위치 정보 연동 최적화.
    * **[추가]** 네트워크 불안정 지역 대응을 위한 로컬 캐싱(Offline Support) 적용.
* **5.3 딥링크(Deep Link) 설정:**
    * 알림 클릭 시 특정 수리 요청 화면으로 즉시 이동하는 기능.

### Phase 6: 스토어 등록 및 최종 심사 (Release & QA)
* **6.1 구글 플레이 콘솔 준비:**
    * 구글 개발자 계정 생성 및 결제.
    * 개인정보처리방침(Privacy Policy) 최종본 등록 및 앱 내 링크 연결.
* **6.2 내부 테스트 및 QA (Closed Testing):**
    * **[중요]** 구글 플레이 신규 계정 정책에 따른 '20인 이상 테스터, 14일 유지' 요건 이행.
    * 실제 현장 버그 수정 및 앱 안정성(Crash rate) 관리.
* **6.3 공식 스토어 심사 제출:**
    * 앱 설명, 스크린샷 등록 및 최종 AAB(App Bundle) 업로드.
    * 구글 심사 대응 및 정식 출시 완료.

---

## 6. 현재 운영 상태 점검 (2026-05-11 기준)

신규 합류 AI(또는 차후 세션의 Sonnet)가 작업을 이어받기 전에 반드시 확인해야 할 **실제 인프라 상태**와 **계획서와의 차이점**입니다. 계획만 보고 작업하면 충돌이 발생합니다.

### 6.1 AWS 인프라 현황과 격차
- **EC2**: `13.124.100.75` (t3.micro, Ubuntu 22.04 LTS) — 운영 중
- **DB 격차 ⚠️**: `AWS_STARTUP_GUIDE.md`에는 백엔드 DB가 `washer_crm.db` (SQLite)로 명시되어 있으나, blueprint 3.2에서는 RDS PostgreSQL을 정의함. **결제·정산 트래픽이 들어오기 전에 RDS PostgreSQL로 전환 필수.**
- **인스턴스 사이즈 격차 ⚠️**: t3.micro(1GB RAM)는 PG 콜백 + WebSocket + Nginx 동시 운영에 부족. 결제 모듈 도입 시점에 최소 t3.small, 권장 t3.medium으로 업그레이드.
- **HTTPS/도메인 미적용 ⚠️**: Public IP로만 접근 중. 포트원 결제 심사는 도메인 + HTTPS + 푸터 필수정보 노출이 통과 조건.
- **포트 개방 현황**: 22, 80, 443, 8000 — 8000은 운영 안정화 후 차단 권장(ALB/Nginx 경유로 통일).

### 6.2 사람 트랙 진행 현황 (human_preparation_guide.md 참조)
사장님 명의로 진행 중인 행정 작업들. **AI는 이 항목들의 완료 여부에 따라 다음 단계 착수 가능 여부를 판단해야 함.**
- [ ] 통신판매업 신고 (정부24, 아버님 명의) — *진행 중*
- [ ] 전자세금계산서용 인증서 발급 (주거래 은행, 법인/대표자 명의) — *진행 중*
- [ ] 도메인 구매 (`whiteon.kr` 등) — *진행 중*
- [x] AWS 계정 (결제수단 등록 완료)
- [ ] 포트원(PortOne) 가입 및 심사 (결제 + 본인인증) — *진행 중*
- [ ] 팝빌(Popbill) 가입 및 인증서 등록 — *진행 중*

### 6.3 격차 해소를 위한 우선 조치
1. EC2에 SSH 접속하여 실제 DB 종류 확인 → RDS PostgreSQL 전환(또는 EC2 내 PostgreSQL 컨테이너로 일단 통일).
2. Route 53 + ACM + Nginx(certbot)로 HTTPS 적용. 포트원 심사 통과 조건 충족.
3. 푸터에 사업자정보·통신판매업 신고번호·이용약관·개인정보처리방침 링크 노출.

---

## 7. 정산·회계·세무 자동화 모듈 상세 설계

Phase 2~3에서 구현할 핵심 모듈입니다. **잘못 짜면 추후 수정이 매우 어려운 영역**이므로 사전 설계를 명시합니다.

### 7.1 정산(Settlement) 데이터 모델 (요지)
- `settlements` (id, branch_id, period_start, period_end, gross_amount, commission_amount, vat_amount, net_amount, status, approved_at, paid_at)
  - status: `DRAFT` → `REVIEW` → `APPROVED` → `PAID` → `INVOICED`
- `settlement_items` (id, settlement_id, payment_id, type[`SERVICE`|`PARTS`|`DETERGENT`|`REFUND_OFFSET`], amount)
- `tax_invoices` (id, settlement_id, popbill_mgt_key, issued_at, status, original_invoice_id) — `original_invoice_id`가 NOT NULL이면 수정세금계산서

### 7.2 정산 비즈니스 규칙 (필수 케이스)
- **부가세 처리**: 결제 금액은 부가세 포함(VAT-inclusive) 수신. 정산 시 net_amount = gross_amount × (1 - commission_rate). 세금계산서 발행은 net_amount 기준의 별도 공급가액/세액 분리 표시.
- **환불·부분환불 처리**: 정산 확정 후 발생한 환불은 다음 정산 주기에 `REFUND_OFFSET` 항목으로 차감 반영. 누적 차감으로 음수 정산이 발생하면 시스템이 보류(`HOLD`) 상태로 두고 본사 승인 후 차회 이월.
- **수정세금계산서**: 환불·금액 변동 발생 시 Popbill API의 수정세금계산서 발행 흐름을 자동 트리거. 원본 invoice_id를 반드시 연결.
- **정산 주기**: 주 단위(월~일 마감, 익주 화요일 지급) 기본. 운영 안정화 후 월 단위 옵션 추가 가능하도록 `period_type` 컬럼 확장 여지 확보.

### 7.3 PG/세무 외부 연동 흐름
- **포트원 결제**: 클라이언트 결제 → 포트원 webhook → 백엔드 검증(amount·imp_uid) → `payments.status = PAID`. 검증 실패 시 즉시 환불 자동화.
- **포트원 본인인증(PASS)**: 가입 시 식당 사장 / 지사 매니저 본인확인 → 휴대폰·생년월일 일치 검증 → `users.is_verified = true`.
- **팝빌 세금계산서**: 정산 `APPROVED` 시 발행 큐에 적재 → 비동기 워커가 Popbill API 호출 → 결과 webhook 또는 폴링 → `tax_invoices.status = ISSUED`. 실패 시 3회 재시도 후 본사 알림.

### 7.4 회계 시스템 연계 (참고)
- 1차 목표: 월말 회계 마감용 CSV/Excel 리포트 자동 생성(매출, 수수료, VAT, 지사별 정산 집계).
- 2차 목표(선택): 더존/이카운트 등 외부 회계 SW와 API/CSV 인터페이스. 사장님이 사용하시는 회계 프로그램이 결정되면 그에 맞게 설계.

### 7.5 분쟁·감사 대응
- 모든 정산·결제·세금계산서 발행 이벤트는 `AuditLog`에 변경 전/후 payload(JSONB) 기록. (`plan_phase1.2.1_versioning_logging.md` 체계 활용)
- 정산서 PDF 스냅샷을 S3에 보관(법적 보관 5년).

---

## 8. 에이전트(Agent) 활용 전략

Claude Agent SDK 기반의 백엔드 워커 형태로 구현. 처음부터 다 만들지 말고 **(1)번부터 단계적으로 도입**.

### 8.1 우선순위별 에이전트 목록
1. **정산 검토 에이전트 (최우선)**: 매주 정산 확정 직전 자동 실행. 지사별 매출/수수료/VAT 계산 검증, 이상치(평균 대비 ±3σ) 탐지, 환불 미반영 검출. 결과를 사장님 카톡/대시보드에 "이상 없음 / N건 확인 필요" 형태로 요약.
2. **A/S 접수 분류·우선순위 에이전트**: 식당이 올린 사진+짧은 텍스트를 분석해 긴급도(영업 중단 여부) 판단, 필요 부품 예측, 적합 지사 자동 매칭. Vision 활용.
3. **Hotspot 분석 에이전트**: 월/주 단위로 실행. 동일 식당 30일 내 동일 증상 재접수, 특정 지역 같은 부품 다발 고장 등 패턴 보고서 생성.
4. **세금계산서 발행 검증 에이전트**: Popbill 발행 결과 사후 검증(금액·사업자번호 매칭). 실패 시 재발행 트리거 또는 본사 알림.
5. **고객 응대 1차 봇**: 카톡채널/앱에서 정형 질의(ETA, 재고, 결제내역) 80% 처리. 비정형 문의만 본사 에스컬레이션. Haiku 권장.

### 8.2 에이전트 구현 가드레일
- 각 에이전트는 **명시적으로 허용된 도구(read_settlement, send_alert 등)만** 사용. DB 쓰기는 가능한 한 별도 승인 단계 거침.
- 모든 에이전트 실행 결과는 `AuditLog`에 기록(어떤 도구를 어떤 인자로 호출했는지 포함).
- 정산·세무 등 금전 관련 에이전트는 **반드시 사람 승인(HQ Dashboard 클릭)** 후 실제 액션 실행. 에이전트는 "초안 생성"까지만.

---

## 9. 모델 사용 정책 (Opus / Sonnet / Haiku)

### 9.1 기본 원칙
- **일상 개발(CRUD, 화면, 버그픽스, 마이그레이션, 테스트)**: Sonnet 4.6 — 가성비·속도 우수.
- **아키텍처 설계 및 한 번 잘못 짜면 되돌리기 어려운 결정**: Opus 4.6 — 정산 알고리즘, 권한·인증 흐름, PG/세무 연동 흐름, 에이전트 시스템 프롬프트 설계.
- **실시간/대량 호출 워커(고객 응대 봇, 단순 분류)**: Haiku 4.5 — 비용·지연시간 최적.

### 9.2 교차검증 규칙
"이 결정 잘못하면 데이터 다시 깔아야 할 수도 있다" 싶은 순간엔 **Sonnet으로 작업한 결과를 Opus에 한 번 더 던져 리뷰**. 구체적으로:
- DB 스키마 변경(특히 `payments`, `settlements`, `tax_invoices` 관련)
- 권한·JWT·본인확인 로직
- Popbill 세금계산서 발행/수정 흐름
- 에이전트의 도구 권한 설계

### 9.3 비용 가이드
- Opus는 Sonnet 대비 약 5배 비용. 위 9.1 분류를 기본으로 따르되, 한 세션 내에서 Opus를 30분 이상 연속 사용 중이라면 작업이 잘못된 방향일 가능성을 의심하고 작업 분해를 재고.

---

## 관련 문서
- [작업 실행 계획 (workplan.md)](./workplan.md) — Sonnet이 다음 세션에서 보고 바로 실행하는 단계별 체크리스트
- [작업 진행 현황 (work_schedule.md)](./work_schedule.md)
- [이슈 관리 및 금기 사항 (harnes.md)](./harnes.md)
- [사람의 행정 준비 가이드 (human_preparation_guide.md)](./human_preparation_guide.md)
- [AWS 운영 가이드 (AWS_STARTUP_GUIDE.md)](./AWS_STARTUP_GUIDE.md)
